{
  "name": "master-gun",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native --host 127.0.0.1",
    "build": "vite build --configLoader native",
    "preview": "vite preview --configLoader native --host 127.0.0.1",
    "lint": "eslint .",
    "test": "vitest run --configLoader native",
    "test:e2e": "node tests/e2e/smoke.mjs",
    "quality": "node scripts/check-quality.mjs",
    "check": "pnpm lint && pnpm test && pnpm build && pnpm test:e2e && pnpm quality"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.38.0",
    "eslint": "^9.38.0",
    "playwright": "^1.56.0",
    "vite": "^7.1.12",
    "vitest": "^3.2.4"
  }
}
