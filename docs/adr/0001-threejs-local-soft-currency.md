# Use Three.js With Local Soft Currency

Master Gun uses plain JavaScript, Vite, and Three.js with DOM overlays because the game needs direct 3D scene control while keeping UI text cheap and accessible. Purchases are local soft-currency upgrades only, so the first release avoids payment, account, fraud, and backend complexity while preserving the arcade upgrade loop.
